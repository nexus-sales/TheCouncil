import { NewDecisionContent } from "@/components/NewDecisionContent";

export const metadata = {
  title: "Nueva decisión / New Decision - The Council",
  description: "Submit a decision, product, or strategy to The Council v2.",
};

export default function NewDecisionPage() {
  return <NewDecisionContent />;
}
